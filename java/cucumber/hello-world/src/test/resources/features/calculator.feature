Feature: Sum of Integer Numbers
  Addition of integer number which are passed as argument. If no argument is passed -1 will be returned

  Scenario: Sum Integer Number
    Given there are two Integers
    When Two number are passed as arguments to method
    Then We can sum those numbers and return result
