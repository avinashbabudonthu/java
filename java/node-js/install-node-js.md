# Installing node
* Download latest version of node from - https://nodejs.org/en/
* Install
* Check node version
```
node --version
node -v
```
* `npm` also installed when we install `node js`
* Check npm version
```
npm --version
npm -v
```