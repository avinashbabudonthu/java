package com.junit5;

import java.util.stream.Stream;

/**
 * JUnit 5 Dynamic Tests feature, which allows to declare and run test cases generated at run-time
 * Dynamic Tests allow us to define the tests case dynamically in the runtime
 * 
 * @author Avinash Babu Donthu
 *
 */
public class DynamicTest {

	public Stream<DynamicTest> generateDynamicTestFromStream() {
		return null;
	}
}
