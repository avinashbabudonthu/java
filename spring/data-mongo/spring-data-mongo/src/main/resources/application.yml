server.port: 9000

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: database1

# log mongo queries
logging.level.org.springframework.data.mongodb.core.MongoTemplate: DEBUG