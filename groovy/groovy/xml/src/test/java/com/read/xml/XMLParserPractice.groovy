package com.read.xml

import org.junit.Test

class XMLParserPractice {

    /**
     * 1. XMLParser is high level API makes it easy to work with XML in groovy
     * 2. Eagerly evaluates XML document. Means loads entire document into memory before it's read
     * 3. Since eagerly evaluation, can be used with case while parsing large xml documents
     *
     */
    @Test
    void xmlParser(){

    }
}
